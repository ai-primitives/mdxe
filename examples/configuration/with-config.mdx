---
title: Configuration Example
---

import { FiGithub } from 'react-icons/fi'

# Configuration Examples

This example demonstrates various configuration options:

## Style Customization

The content width and colors are customized via mdxe.config.js.

## Remote Imports

Using a remote icon component:

<FiGithub size={24} />

## Watch Mode

The file is automatically recompiled on changes when watch mode is enabled.

## Configuration Options

You can configure mdxe through:
1. package.json (`mdxe` field)
2. mdxe.config.js
3. .mdxerc
4. .mdxerc.json
5. .mdxerc.yaml
